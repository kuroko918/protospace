$('#js-comments_count').html("<%= "Comments (#{@comments.count})" %>");
$('#js-comment_list').html("<%= j(render partial: 'comments/comment', locals: { comments: @comments }) %>");
$('#js-comment_text').val('');
